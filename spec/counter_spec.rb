require 'spec_helper'

module Watchwords
  describe Counter do
    describe ".count" do
      it "does not throw" do
        opts = {}
        raw_count = Counter.count opts
      end
    end
  end
end
